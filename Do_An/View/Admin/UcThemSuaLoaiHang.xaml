<UserControl x:Class="Do_An.View.Admin.UcThemSuaLoaiHang"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#FFF8F5F3">

    <UserControl.Resources>

        <!-- ===== Màu dùng chung ===== -->
        <SolidColorBrush x:Key="CardBorderBrush" Color="#FFE6DAD4"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#FF5C3B31"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#FF8D7B74"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#FFFFFAF8"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#FFE8DCD5"/>
        <SolidColorBrush x:Key="SaveButtonBrush" Color="#FFB58A6A"/>
        <SolidColorBrush x:Key="CancelButtonBrush" Color="#FF8B6251"/>
        <SolidColorBrush x:Key="ErrorTextBrush" Color="#FFD9534F"/>

        <!-- ===== Button ===== -->
        <Style x:Key="FormButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== TextBox ===== -->
        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <!-- ===== Error ===== -->
        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="4,6,0,0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource ErrorTextBrush}"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="#FFF8F5F3">

        <Border Margin="18"
                Padding="24"
                Background="White"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="20"
                VerticalAlignment="Top">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="28"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ===== Tiêu đề ===== -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="{Binding TieuDe}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryTextBrush}"/>

                    <TextBlock Text="Nhập thông tin loại hàng bên dưới. Những mục có dấu * là bắt buộc."
                               Margin="0,8,0,0"
                               FontSize="14"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                </StackPanel>

                <!-- ===== Form nhập thông tin ===== -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Mã loại hàng -->
                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Mã loại hàng *"
                                   Margin="4,0,0,6"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>

                        <Border Height="44"
                                Background="{StaticResource InputBackgroundBrush}"
                                BorderBrush="{StaticResource InputBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="16">
                            <TextBox Style="{StaticResource FormTextBoxStyle}"
                                     Text="{Binding MaLoaiHang, UpdateSourceTrigger=PropertyChanged}"
                                     IsReadOnly="{Binding IsEdit}"/>
                        </Border>

                        <TextBlock Text="{Binding ErrorMaLoaiHang}"
                                   Style="{StaticResource ErrorTextStyle}"/>
                    </StackPanel>

                    <!-- Tên loại hàng -->
                    <StackPanel Grid.Row="0" Grid.Column="2">
                        <TextBlock Text="Tên loại hàng *"
                                   Margin="4,0,0,6"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>

                        <Border Height="44"
                                Background="{StaticResource InputBackgroundBrush}"
                                BorderBrush="{StaticResource InputBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="16">
                            <TextBox Style="{StaticResource FormTextBoxStyle}"
                                     Text="{Binding TenLoaiHang, UpdateSourceTrigger=PropertyChanged}"/>
                        </Border>

                        <TextBlock Text="{Binding ErrorTenLoaiHang}"
                                   Style="{StaticResource ErrorTextStyle}"/>
                    </StackPanel>

                    <!-- Gợi ý -->
                    <Border Grid.Row="2"
                            Grid.Column="0"
                            Grid.ColumnSpan="3"
                            Background="{StaticResource InputBackgroundBrush}"
                            BorderBrush="{StaticResource InputBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="16"
                            Padding="16">

                        <TextBlock Text="Mã loại hàng nên ngắn gọn, dễ tra cứu. Tên loại hàng nên rõ nghĩa để thuận tiện khi phân loại hàng hóa."
                                   FontSize="13"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   TextWrapping="Wrap"/>
                    </Border>
                </Grid>

                <!-- ===== Nút chức năng ===== -->
                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">

                    <Button Content="Lưu"
                            Style="{StaticResource FormButtonStyle}"
                            Background="{StaticResource SaveButtonBrush}"
                            Command="{Binding LuuCommand}"/>

                    <Button Content="Hủy"
                            Margin="40,0,0,0"
                            Style="{StaticResource FormButtonStyle}"
                            Background="{StaticResource CancelButtonBrush}"
                            Command="{Binding HuyCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>