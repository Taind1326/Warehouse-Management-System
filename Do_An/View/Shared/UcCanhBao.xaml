<UserControl x:Class="Do_An.View.Shared.UcCanhBao"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="FilterToggleStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="86"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF6B463B"/>
            <Setter Property="BorderBrush" Value="#FFE6DAD4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FFE8DAD4"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FFF3E9E4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SeenButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="78"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="#FFB58A6A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.78"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White"
            BorderBrush="#FFE6DAD4"
            BorderThickness="1"
            CornerRadius="18"
            Padding="22">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <StackPanel Margin="0,0,150,0">
                    <TextBlock Text="Cảnh báo tồn kho"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="#FF4F3127"/>

                    <TextBlock Text="Theo dõi các mặt hàng dưới mức tồn tối thiểu"
                               Margin="0,6,0,0"
                               FontSize="14"
                               Foreground="#FF8D7B74"/>
                </StackPanel>

                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="{Binding BadgeNen}"
                        BorderBrush="{Binding BadgeVien}"
                        BorderThickness="1"
                        CornerRadius="18"
                        Padding="14,7">

                    <TextBlock Text="{Binding BadgeText}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{Binding BadgeMau}"/>
                </Border>
            </Grid>

            <Border Grid.Row="1"
                    Margin="0,20,0,16"
                    Background="#FFFFFAF7"
                    BorderBrush="#FFEFE1D9"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="14">

                <WrapPanel>
                    <ToggleButton Content="Tất cả"
                                  IsChecked="{Binding FilterTatCa, Mode=OneWay}"
                                  Command="{Binding FilterCommand}"
                                  CommandParameter="all"
                                  Style="{StaticResource FilterToggleStyle}"/>

                    <ToggleButton Content="Chưa xem"
                                  IsChecked="{Binding FilterChuaXem, Mode=OneWay}"
                                  Command="{Binding FilterCommand}"
                                  CommandParameter="chua"
                                  Style="{StaticResource FilterToggleStyle}"/>

                    <ToggleButton Content="Đã xem"
                                  IsChecked="{Binding FilterDaXem, Mode=OneWay}"
                                  Command="{Binding FilterCommand}"
                                  CommandParameter="da"
                                  Style="{StaticResource FilterToggleStyle}"/>
                </WrapPanel>
            </Border>

            <ListBox Grid.Row="2"
                     ItemsSource="{Binding DanhSachHienThi}"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     HorizontalContentAlignment="Stretch">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,10"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFFFFAF7"
                                BorderBrush="#FFEFE1D9"
                                BorderThickness="1"
                                CornerRadius="14"
                                Padding="14"
                                Opacity="{Binding ItemOpacity}">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="42"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="86"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Width="34"
                                        Height="34"
                                        CornerRadius="12"
                                        Background="{Binding IconNen}"
                                        VerticalAlignment="Top">

                                    <TextBlock Text="{Binding IconText}"
                                               FontSize="15"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1"
                                            Margin="10,0,10,0">

                                    <TextBlock Text="{Binding NOIDUNG}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{Binding NoiDungMau}"
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis"
                                               MaxHeight="42"/>

                                    <TextBlock Text="{Binding NgayTaoHT}"
                                               Margin="0,5,0,0"
                                               FontSize="11"
                                               Foreground="#FF8D7B74"/>
                                </StackPanel>

                                <Button Grid.Column="2"
                                        Content="Đã xem"
                                        Style="{StaticResource SeenButtonStyle}"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding NutHienThi}"
                                        Command="{Binding DataContext.DanhDauCommand,
                                                  RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding MACANHBAO}"/>

                                <TextBlock Grid.Column="2"
                                           Text="Đã xem"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Foreground="#FFB8A9A2"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           Visibility="{Binding DaXemHienThi}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>